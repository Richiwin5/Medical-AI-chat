# render.yaml
services:
  - type: web
    name: hospital-ai
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      chmod +x setup.sh
      ./setup.sh
    startCommand: gunicorn api:app --bind 0.0.0.0:$PORT